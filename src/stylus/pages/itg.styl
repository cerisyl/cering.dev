.itg
  flex(row, wrap, start, gap: 8px)
  margin-bottom 16px
  &-main
    &-inner
      width calc(50% - 8px)
      height 100%
      hover-highlight()
      border-radius 4px
    &-image
      source, img
        width 100%
        border-radius 4px